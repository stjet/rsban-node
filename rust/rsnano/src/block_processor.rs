use crate::UncheckedInfo;

pub(crate) struct BlockProcessor {}

impl BlockProcessor {
    pub(crate) fn new() -> Self {
        Self {}
    }

    pub(crate) fn add(&self, _info: &UncheckedInfo) {}
}
